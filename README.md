# Liquid Glass

A premium mobile app with liquid glass morphism UI.

## Deploy to Vercel

```bash
npm install
npx expo export:web
vercel --prod --cwd web-build
```

Or connect your GitHub repo to Vercel and it will auto-deploy.
